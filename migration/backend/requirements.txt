# ── FastAPI core ────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2

# ── Database (async) ────────────────────────────────────────────
sqlalchemy==2.0.35
asyncpg==0.29.0
aiosqlite==0.20.0          # local dev: run against the desktop SQLite file
alembic==1.13.3

# ── Data port (one-time SQLite → Postgres script) ───────────────
# migration/database/migrate_sqlite_to_postgres.py uses a *sync* driver
# (psycopg2) + dotenv to read DATABASE_URL from .env.
psycopg2-binary==2.9.9
python-dotenv==1.0.1

# ── Auth ────────────────────────────────────────────────────────
python-jose[cryptography]==3.3.0
# bcrypt used directly (passlib 1.7.x is incompatible with bcrypt 4.1+).
bcrypt==4.2.0
python-multipart==0.0.12

# ── Excel import (reused from desktop importer.py) ──────────────
openpyxl==3.1.5

# ── Dev / testing ───────────────────────────────────────────────
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
