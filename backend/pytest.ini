[pytest]
# Async tests run via asyncio.run() inside the test functions themselves, so we
# don't need pytest-asyncio's auto mode — but set the loop scope explicitly to
# silence the deprecation warning if the plugin is installed.
asyncio_mode = strict
asyncio_default_fixture_loop_scope = function

testpaths = tests
addopts = -q
